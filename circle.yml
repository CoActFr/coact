checkout:
    post:
        - git submodule sync
        - git submodule update --init
test:
    pre:
        - npm start:
            background: true
        - sleep 5
deployment:
  production:
    branch: master
    commands:
      - npm run deploy:prod
  staging:
    branch: staging
    commands:
      - npm run deploy:preprod
